Hello {{ user }} :marvin:

{%- if status == "cancelled" %}
Alas, the universe has erased your namespace `{{ target_namespace }}` because its originating pipeline was superseded by another namespace, manually deleted, or cancelled.
{%- else %}
Alas, the universe has whimsically erased your namespace `{{ target_namespace }}` for being `{{ status }}` [{{ status_timeframe }}].
{%- endif %}
Not that it would make much difference in the grand scheme of things, but should you find yourself curiously baffled, consult your <{{ job_url }}|pipeline job> for some clues buried in monitoring and logging speckles of dust.

"_{{ quote }}_"
