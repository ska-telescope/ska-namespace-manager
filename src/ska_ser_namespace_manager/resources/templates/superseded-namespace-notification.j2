Hello {{ user }} :marvin:

Your namespace `{{ target_namespace }}` is now `{{ status }}`. Not that it matters. A newer deployment for the same branch has turned up, all bright-eyed and temporarily meaningful, so this older namespace will be deleted shortly.

You may inspect the <{{ job_url }}|pipeline job> for the final, faint traces of what happened, assuming you enjoy staring into the machinery as it tidies away another tiny disappointment.

"_{{ quote }}_"
